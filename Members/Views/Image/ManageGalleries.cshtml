@model IEnumerable<Members.Models.GalleryViewModel>
@using Members.Models

@{
    ViewData["Title"] = "Manage Image Galleries";
}

<!-- Title: Manage Galleries -->
<h1 class="text-center text-gold-darker my-2"><i class="bi bi-images"></i> Manage Galleries</h1>

<!-- Back to Galleries Button -->
<div class="text-center mb-3">
    <a asp-controller="Image" asp-action="GalleryList" class="btn btn-sm btn-back"><i class="bi bi-box-arrow-left"></i> Back to Galleries</a>
</div>

@if (TempData["SuccessMessage"] != null)
{
    @* <div class="alert alert-success">@TempData["SuccessMessage"]</div> *@
}
@if (TempData["ErrorMessage"] != null)
{
    @* <div class="alert alert-danger">@TempData["ErrorMessage"]</div> *@
}

<!-- Row and Column setup for Card -->
<div class="container row justify-content-center mx-auto p-1">
    <div class="col-lg-6 col-md-10 m-1 p-1">

        <!-- Shaddowed Card -->
        <div class="card shadow rounded border bg-Card-Menu">
            <div class="card-body p-2">

                <!-- Heading: Create a New Gallery -->
                <h2 class="text-start text-gold-darker my-2 ps-2"><i class="bi bi-folder-plus"></i> Create a New Gallery</h2>

                <!-- Form for Creating a new Gallery -->
                <form asp-controller="Image" asp-action="CreateGallery" method="post" class="mb-4">

                    <!-- New Gallery Name and Save Button -->
                    <div class="input-group">

                        <!-- New Name -->
                        <input type="text" name="GalleryName" class="form-control text-gold-darker shadow" placeholder="New Gallery Name" required>
    
                        <!-- Save New Gallery -->
                        <span data-bs-toggle="tooltip" data-bs-placement="top" title="Save New Gallery">
                            <button type="submit" class="btn btn-primary shadow no-left-radius">
                                <i class="bi bi-save"></i>
                            </button>
                        </span>

                    </div>

                    <span class="text-danger">@Html.ValidationMessage("GalleryName")</span>

                </form>

                @if (Model != null && Model.Any())
                {
                    <h2 class="text-start text-gold-darker my-2 ps-2"><i class="bi bi-images"></i> Existing Galleries</h2>

                    <table class="table table-striped table rounded shadow">
                        <thead>
                            <tr>
                                <th class="text-gold-darker">Gallery</th>
                                <th class="text-gold-darker">Images</th>
                                <th class="text-gold-darker">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var gallery in Model)
                            {
                                <tr>

                                    <td class="text-gold-darker">@gallery.Name</td>
                                    <td class="text-gold-darker">@gallery.ImageCount</td>
                                    <td>
                                        <a asp-controller="Image" asp-action="ManageGalleryImages" asp-route-galleryName="@gallery.Name"
                                           class="btn btn-sm btn-info"
                                           data-bs-toggle="tooltip" data-bs-placement="top" title="Manage Images for @gallery.Name">
                                            <i class="bi bi-images"></i>
                                        </a>

                                        <span data-bs-toggle="tooltip" data-bs-placement="top" title="Rename Gallery @gallery.Name">
                                            <button type="button" class="btn btn-sm btn-warning"
                                                    data-bs-toggle="modal" data-bs-target="#renameGalleryModal_@gallery.Name">
                                                <i class="bi bi-pencil"></i>
                                            </button>
                                        </span>

                                        <form asp-controller="Image" asp-action="DeleteGallery" method="post" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete the gallery \'@gallery.Name\' and all its images?');">
                                            <input type="hidden" name="galleryName" value="@gallery.Name" />
                                            <button type="submit" class="btn btn-sm btn-danger"
                                                    data-bs-toggle="tooltip" data-bs-placement="top" title="Delete Gallery @gallery.Name">
                                                <i class="bi bi-x-circle"></i>
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                    <!-- Move modal definitions outside of the tbody to fix the nesting issue -->
                    @foreach (var gallery in Model)
                    {
                        <div class="modal fade" id="renameGalleryModal_@gallery.Name" tabindex="-1" aria-labelledby="renameGalleryModalLabel_@gallery.Name" aria-hidden="true">
                            <div class="modal-dialog">
                                <div class="modal-content">

                                    <form asp-controller="Image" asp-action="RenameGallery" method="post">
                                        <div class="modal-header">
                                            <h5 class="modal-title" id="renameGalleryModalLabel_@gallery.Name">Rename Gallery: @gallery.Name</h5>
                                            <span data-bs-toggle="tooltip" data-bs-placement="top" title="Close">
                                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                            </span>
                                        </div>
                                        <div class="modal-body">
                                            <input type="hidden" name="oldGalleryName" value="@gallery.Name" />
                                            <div class="mb-3">
                                                <label for="newGalleryName_@gallery.Name" class="form-label">New Gallery Name:</label>
                                                <input type="text" class="form-control" id="newGalleryName_@gallery.Name" name="newGalleryName" value="@gallery.Name" required>
                                            </div>
                                        </div>

                                        <!-- Buttons -->
                                        <div class="modal-footer">

                                            <span data-bs-toggle="tooltip" data-bs-placement="top" title="Close Window">
                                                <button type="button" class="btn btn-sm btn-secondary" data-bs-dismiss="modal">
                                                    <i class="bi bi-box-arrow-left me-2"></i>Close
                                                </button>
                                            </span>

                                            <span data-bs-toggle="tooltip" data-bs-placement="top" title="Apply Rename">
                                                <button type="submit" class="btn btn-sm btn-primary">
                                                    <i class="bi bi-pencil me-2"></i> Rename
                                                </button>
                                            </span>

                                        </div>

                                    </form>

                                </div>
                            </div>
                        </div>
                    }
                }
                else
                {
                    <div class="row gx-1 gy-0 my-2">
                        <div class="col-md-8 text-center text-gold-dark mx-auto">
                            <p>No galleries exist yet. Create one above!</p>
                        </div>
                    </div>
                }

            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        /* Custom styles for this page only */
        .no-right-radius {
            border-top-right-radius: 0 !important;
            border-bottom-right-radius: 0 !important;
        }

        .no-left-radius {
            border-top-left-radius: 0 !important;
            border-bottom-left-radius: 0 !important;
        }
    </style>
    }

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}