@model IEnumerable<Members.Models.PDFCategory>

@{
    ViewData["Title"] = "PDF Categories"; // This will be set in the controller, but good practice to have a default
    Layout = "~/Views/Shared/_Layout.cshtml"; // Use your main layout
}

<div class="text-center mt-4 mb-2 text-gold-darker">
    <h1><i class="bi bi-file-earmark-pdf-fill me-0 text-gold-darker"></i> @ViewData["Title"]</h1>
</div>

<div class="container row justify-content-center mx-auto p-1">
    <div class="col-lg-6 col-md-10 m-1 p-1">
        <div class="card shadow rounded border bg-PDF-Menu">
            <div class="card-body p-0">
                @if (Model != null && Model.Any())
                {
                    <ul class="list-group list-group-flush">
                    @foreach (var category in Model)
                    {
                        <li class="list-group-item bg-transparent text-gold-darker" style="cursor: pointer;">
                            <a asp-controller="Members" asp-action="ListFiles" asp-route-categoryId="@category.CategoryID" class="text-decoration-none text-gold-darker d-block">
                                    <i class="bi bi-list-ul me-0 text-gold-darker"></i> @category.CategoryName
                            </a>
                        </li>
                    }
                    </ul>
                }
                else
                {
                    <p class=" mt-0 pt-0 text-center text-gold-darker">No categories with files found.</p>
                }
            </div>
        </div>
    </div>
</div>

@section Styles {
        <style>
            /* Styles to remove the borders from the list items */
            .card-body .list-group-item {
                border-bottom: none !important;
            }
             .card-body .list-group-item:first-child {
                 border-top: none !important;
             }
             .card-body .list-group-item:last-child {
                  border-bottom: none !important;
             }

            /* CSS to control the link color on hover/focus */
            /* Target the link directly within the list item on hover/focus */
            .card-body .list-group-item:hover a.text-gold-darker,
            .card-body .list-group-item:focus a.text-gold-darker,
            .card-body .list-group-item a.text-gold-darker:hover,
            .card-body .list-group-item a.text-gold-darker:focus {
                color: #3b3726 !important; /* Use the dark gold color */
            }

            /* Optional: Adjust the background color of the list item on hover/focus */
            .card-body .list-group-item:hover,
            .card-body .list-group-item:focus {
                 background-color: rgba(0, 0, 0, 0.05) !important; /* Example subtle background */
            }
        </style>
}

@section Scripts {
<!-- Add any necessary scripts here, e.g., for styling or minor interactions -->
}